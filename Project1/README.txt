Please have the input be in a file titled "input.txt" in the same directory as the executable \
beacuse the file will be read through the function

File file = new File("./input.txt");

Notes:
HillClimbing random restart condition = 100 iterations of the loop.